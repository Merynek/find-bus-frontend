export enum MapBoxSources {
    MARKERS = "MARKERS",
    DIRECTIONS = "DIRECTIONS"
}

export enum MarkerImages {
    PLACE = "PLACE"
}

export enum MarkerLayers {
    PLACE = "PLACE"
}

export enum PolylineLayers {
    BASE = "BASE"
}